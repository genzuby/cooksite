import React from 'react';
import styled from 'styled-components';

const CardMenu = () => {
  return <CARDMENU>Card Menu</CARDMENU>;
};

const CARDMENU = styled.div`
  grid-area: menu;
  padding: 2em;
`;

export default CardMenu;
